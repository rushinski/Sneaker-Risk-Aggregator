{
  "name": "sneaker-risk-aggregator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:client": "cd client && react-scripts start",
    "start:server": "cd server && nodemon server.js",
    "dev": "concurrently \"npm run start:client\" \"npm run start:server\"",
    "build": "cd client && react-scripts build"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.0",
    "express": "^4.18.2",
    "mongoose": "^7.6.0",
    "morgan": "^1.10.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "^0.0.0",
    "tailwindcss": "^3.3.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "nodemon": "^3.0.1"
  }
}
